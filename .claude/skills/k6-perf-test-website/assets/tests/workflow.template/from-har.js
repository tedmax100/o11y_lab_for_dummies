// from-har.js -- raw har-to-k6 output, committed for audit.
//
// This file is REGENERATED by:
//   npx har-to-k6 recordings/har/<WORKFLOW_PLACEHOLDER>.har -o tests/<WORKFLOW_PLACEHOLDER>/from-har.js
//
// You should NOT edit this file by hand. Instead, hand-clean it into
// `protocol.js` per the k6-perf-test-website skill's functional-tests.md
// procedure:
//
//   1. Move per-request User-Agent / sec-ch-ua-* to options.userAgent
//   2. Rename the opaque group
//   3. Parameterise BASE_URL via __ENV
//   4. Replace recorded credentials with test credentials / __ENV
//   5. Drop the auto-inserted sleep(1)
//   6. Add expect() on every load-bearing response
//
// Then commit BOTH the regenerated from-har.js AND the cleaned protocol.js so
// the audit trail is preserved.
//
// THIS FILE IS A PLACEHOLDER. Replace it by regenerating from your HAR.

import http from 'k6/http';
import { sleep, group } from 'k6';

export const options = {
  iterations: 1,
};

export default function () {
  group('page_0', function () {
    // The body of this function will be filled in by `npx har-to-k6`. It will
    // contain one http.* call per request in the HAR, plus auto-inserted
    // headers and a trailing sleep(1).
    //
    // Run the recorder + har-to-k6 to populate this:
    //
    //   node recordings/scripts/<WORKFLOW_PLACEHOLDER>.js
    //   npx har-to-k6 recordings/har/<WORKFLOW_PLACEHOLDER>.har \
    //     -o tests/<WORKFLOW_PLACEHOLDER>/from-har.js
    sleep(1);
  });
}
